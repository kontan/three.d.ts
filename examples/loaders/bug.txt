*Fixed Bug in original collada loader:

	obj = new THREE.PerspectiveCamera(cparams.fov, cparams.aspect_ratio, cparams.znear, cparams.zfar);
	                                          ****
	obj = new THREE.PerspectiveCamera(cparams.xfov, cparams.aspect_ratio, cparams.znear, cparams.zfar);


*class Node 
 		class property 'type' is not initialized in the constructor. 

*class Transform
	        angle not initialized

*input = this.vertices.input.NORMAL;
                            ^^^^^^ NORMAL is not exist.

*case 'yfov':
		this.yfov = param.textContent;
		                  ^^^^^^^^^^^ Type discording 

*getConvertedMember
		getConvertedMember never return value but the expression in getConvertedIndex recieves it.